export interface ProgramType {
    id: number;
    name: string;
    startDate: string;
    endDate: string;
    description: string;
    requirements: string;
    supportBanner: string;
    budgetAmount: string;
    categoryId: number;
    createdAt: string;
    updatedAt: string;
  }

  export interface BlogType {
    id: number;
   title:string;
   description:string;
   image:string;
   createdAt:string;
  }
  